export default function Note() {
  return (
    <div className="note">
      <pre>
        1.)Questions ,if not answered, will be auto-submitted after 20 seconds
        &nbsp;&nbsp;&nbsp;
      </pre>
      <pre>
        2.)For answers with decimal point,2 digits after decimal should suffice.
        &nbsp;&nbsp;&nbsp;
      </pre>
      <pre>
        3.)Refresh will only generate a new question,scores will be
        maintained.Use 'Reset Quiz'to begin again
      </pre>
    </div>
  );
}
